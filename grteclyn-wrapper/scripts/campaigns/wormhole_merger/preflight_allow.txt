# Keys preflight.py may see unread after its 0-step start-up WITHOUT refusing
# the launch: keys read only once the run writes its first plotfile (the probe
# writes none), and keys no code reads at all (dead leftovers in templates).
# One key per line, then "# why" -- an entry without a reason is not allowed,
# because every line here is a hole in the net that caught the 2026-09-23
# seed trap.  Add a key only after checking the code that reads it.
amr.plot_vars              # read by AmrLevel::setPlotVariables() when the first plotfile is written; the probe writes none (amr.plot_files_output = 0)
amr.derive_plot_vars       # same reader as amr.plot_vars
nonzero_asymptotic_vars    # ignored by design: asymptotic values are compile-time, StateVariables.hpp (Source/GRTeclynCore/AMReXParameters.hpp, LegacyBoundaryParams comment)
nonzero_asymptotic_values  # same as nonzero_asymptotic_vars
hdf5_subpath               # no code reads it (no hit in Source/ or Examples/BinaryWormholeMerger; binaries build USE_HDF5=FALSE): a leftover in every template
